import React from 'react';
import { RecoverPasswordComponent } from '../../user/components/recover-password';

export const RecoverPasswordPage: React.FC = React.memo(() => <RecoverPasswordComponent />);
